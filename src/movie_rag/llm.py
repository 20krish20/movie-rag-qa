def call_llm(prompt: str) -> str:
    """
    Replace this stub with your local/colab LLM implementation.
    Must return a string that contains exactly one ```python ... ``` block.
    """
    raise RuntimeError("call_llm() not configured. Implement in src/movie_rag/llm.py")
